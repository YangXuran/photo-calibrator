import type { WorkspaceFile } from "../types";

type FilmstripItemMetaProps = {
  item: WorkspaceFile;
  showDetail: boolean;
  showMeta: boolean;
};

export function FilmstripItemMeta(_props: FilmstripItemMetaProps) {
  return null;
}
